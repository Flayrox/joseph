# Resources

Place the two application logo variants here when preparing a signed release:

- `logo.png`: transparent/outline logo, used without a background.
- `logo_fill.jpg`: filled logo, used on surfaces that provide or require a background.

The current source PNG/JPG files at the repository root are kept as source artwork until an asset catalog is introduced.
